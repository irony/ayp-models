#!monit
set logfile /var/log/monit.log

check process app-web
    start program = "/sbin/start app-web"
    stop program  = "/sbin/stop app-web"
    if failed port 5000 protocol HTTP
        request /
        with timeout 10 seconds
        then restart