#!/bin/sh
touch tmp/restart.txt
echo "restarting All Your Photos after receiving push"

export NODE_ENV=production 
export PORT=3000
export SSL_PORT=443


npm install
npm test
mason build .

sudo stop app
sudo pkill "node"

sudo foreman export upstart /etc/init -u ec2-user
sudo start app
