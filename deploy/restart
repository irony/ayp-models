#!/bin/sh
touch tmp/restart.txt
echo "restarting All Your Photos after receiving push"
npm install
npm test
forever stopall
forever -l app.log start server/server.js 
forever -l worker.log start server/worker.js