#!/bin/sh
touch tmp/restart.txt
echo "restarting All Your Photos after receiving push"

# provisioning 
#sudo gem install mason 
#sudo gem install foreman 
#sudo npm install -g foreman

export NODE_ENV=production
export SSL_PORT=443
export HOST=www.allyourphotos.org
export MONGOHQ_URL=mongodb://R:billion@ec2-54-228-162-49.eu-west-1.compute.amazonaws.com/allyourphotos


npm install
npm test
mason build .

#sudo stop app
#sudo pkill "node"

sudo nf export -u ec2-user -a app
sudo stop app-web
sudo start app-web
sudo chmod 700 deploy/monit
sudo chown root deploy/monit
sudo monit -d 60 -c deploy/monit
#foreman start web=1